var config = {
        server: {
            host: "0.0.0.0",
            port: "8000"
        },
        db: {
            "dbHost": "localhost",
            "dbPort": "27017",
            "database" : "qlever"
        }
};

exports.config = config;
